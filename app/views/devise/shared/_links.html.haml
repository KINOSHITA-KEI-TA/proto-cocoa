- if controller_name != 'sessions'
  = link_to "既に登録済みの方", new_session_path(resource_name), class: 'Button'
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "アカウントをお持ちでない方", new_registration_path(resource_name), class: 'Button'
  %br/
